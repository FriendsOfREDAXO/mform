<?php

// init mform
$mform = new MForm();

// fieldset
$mform->addFieldset('MForm Widgets');

// custom link
$mform->addCustomlinkField(1,array('label' => 'Customlink'));

// img list
$mform->addImagelistField(2, array('label' => 'Imagelist'));

// parse form
echo $mform->show();
